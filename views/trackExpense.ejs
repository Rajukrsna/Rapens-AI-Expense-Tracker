<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Track Spending</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KyZXEJv+DQj5CQ3kF0O5RmpuJ7nmY1+u1jwF4MGd2hHXxAKtsUdbkDB5nMlL2sR8" crossorigin="anonymous">
    <style>
        

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        .card {
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            animation: slideUp 1s ease;
        }

        @keyframes slideUp {
            from {
                transform: translateY(30px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .form-label {
            font-weight: bold;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-8px);
            }
        }

        .btn-secondary {
            background-color: #6c757d;
            border-color: #6c757d;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-secondary:hover {
            background-color: #5a6268;
        }

        .hero-image {
            width: 100%;
            height: auto;
            border-radius: 10px;
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-10">
                <!-- Hero Image -->
                
                <div class="card p-4">
                    <h2 class="text-center text-primary mb-4">Track Your Spending</h2>
                    <p class="text-center text-muted">Add your spending details to keep your budget on track.</p>

                    <!-- Track Spending Form -->
                    <form action="/trackExpense/spend" method="POST">
                        <div class="mb-4">
                            <label for="category" class="form-label">Category</label>
                            <select name="category" id="category" class="form-select" required>
                                <% categories.forEach(category => { %>
                                    <option value="<%= category.name %>"><%= category.name %></option>
                                <% }); %>
                            </select>
                        </div>

                        <div class="mb-4">
                            <label for="amount" class="form-label">Amount Spent ($)</label>
                            <input type="number" name="amount" id="amount" class="form-control" placeholder="Enter amount spent" required>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">Add Spending</button>
                        </div>
                    </form>

                    <!-- Back to Setup Budget Button -->
                    <div class="mt-4 text-center">
                        <a href="/" class="btn btn-secondary">Back to Setup Budget</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and Dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"
        integrity="sha384-oBqDVmMz4fnFO9gybRlu0Hlxh0xAp0xk1iP6M1zqB+KFZz6fM8Bf2mDz0CZHe6J6"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"
        integrity="sha384-pzjw8f+ua7Kw1TIq0v8Fq6wrp88aPtkts0lg0W9kZxKx7OmfBzI9pfE1qXBoF8i7"
        crossorigin="anonymous"></script>
</body>

</html>
